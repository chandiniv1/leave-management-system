fn main(){
    let x:char='z';
    let i:i32=-200;
    let u:u32=200;
    //let v:u32=-2; //results an error
    let f:f32=2.5;
   // let b:bool=1; //results an error
    let s:bool=true;
    println!("x={} i={} u={} f={} s={}",x,i,u,f,s)
}